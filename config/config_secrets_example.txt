# copy this file to 'config_secrets.txt' in the same directory
# 這個檔案是 .gitignore 的，config_secrets.txt 不會進 git
#
# go.sh 用 '.' source 這個檔案，所以是 shell 變數寫法：= 前後不能有空白
# 值有特殊字元的話要用單引號包起來，例如 pw='my p@ss word'

# --- . go.sh 2 (家裡 LAN, rsync daemon port 873) ---
# DS918  admin@192.168.123.163
pw=thepassword
# DS1525 jie@192.168.123.164
pw_1525=thepassword_1525

# --- . go.sh 3 (在外面, 只有 DS918) ---
# 對外的 IP 或 DDNS domain
pi_public=220.10.10.10
# 對外的 port。注意 go.sh 同時把它當 rsync --port 和 ssh -p 用
pp_public=1234
pw_public=thepassword_public

# 註：LAN 的 IP 和帳號 (admin@192.168.123.163, jie@192.168.123.164)
#     是寫死在 go.sh 裡面的，不在這個檔案。
#     只有 . go.sh 2 和 . go.sh 3 需要這個檔案；. go.sh 0 和 1 不用。
